该文档用于构建对数据结构的整体知识体系


① 讲一讲数组
   1. 数组是一种线性的数据结构。在计算机中, 它使用的是一块连续的内存, 来存储一组具有相同类型的元素
   2. 数组的优点就是支持随机访问, 缺点是长度固定
   3. 在Java中, ArrayList和Vector都是基于数组实现的一个动态数组
   4. ArrayList 每次添加新元素时, 都会对容量进行检查, 扩容时则会增长为原来容量的1.5倍, 并且它是非线程安全的, 适合查询、修改频繁的场景
   5. Vector的功能跟ArrayList类似, 不过扩容时会增长为原来的2倍。并且由于它的方法使用到了synchronized关键字, 所以是线程安全的, 但是性能
      要比ArrayList差



------------------------------------------------------------------------------------------------------------------------



